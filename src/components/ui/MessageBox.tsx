import React from "react";

export default function MessageBox({ id, conversation_id, sender_id, content, edited_at, deleted_at, created_at, isReaded }: Message) {
  return (
    <div>
    </div>
  )
}